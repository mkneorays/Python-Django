In Project to use template file 
-----------------------------------------------------
1 > First of all create the project using below command
C:\Users\CHOTKA\Desktop\CorePython\DjangoApps>django-admin startproject ViewTemplate1
2 > Start Application
C:\Users\CHOTKA\Desktop\CorePython\DjangoApps\ViewTemplate1>python manage.py startapp testapp
3 >create one templates folder in our main project only  (be carefull for creating folder )
     in the template folder create a seprate folder with our application name like testapp
     inside folder create our html files nothig but templates
4 > Add application to the project inside settings.py
    Add template folder inside settings.py file
    after that add template dir like TEMPLATE_DIR add inside  TEMPLATES code inside settings.py only like 'DIRS': [TEMPLATE_DIR],
5 > defined view function inside views.py
6 > define url pattern for view function inside urls.py
7 > run server and send request

------------------------------------------------------------------------------------------------------------
urls.py
------
"""ViewTemplate1 URL Configuration

The `urlpatterns` list routes URLs to views. For more information please see:
    https://docs.djangoproject.com/en/2.1/topics/http/urls/
Examples:
Function views
    1. Add an import:  from my_app import views
    2. Add a URL to urlpatterns:  path('', views.home, name='home')
Class-based views
    1. Add an import:  from other_app.views import Home
    2. Add a URL to urlpatterns:  path('', Home.as_view(), name='home')
Including another URLconf
    1. Import the include() function: from django.urls import include, path
    2. Add a URL to urlpatterns:  path('blog/', include('blog.urls'))
"""
from django.contrib import admin
from django.urls import path
from testapp import views

urlpatterns = [
    path('admin/', admin.site.urls),
    path('header/',views.template_view),
]
--------------------------------------------------------------
views.py
--------
from django.shortcuts import render
from  django.http import HttpResponse
import datetime
# Create your views here.
def template_view(request):
    dt=datetime.datetime.now()
    name="Mithilesh kumar"
    roll=101
    marks=200
    my_dict={'date':dt,'name':name,'rollno':roll,'mark':marks}
    # to write dynamic content like tim and date in template
    #return render(request,'testapp/results.html',context=my_dict)
    return render(request,'testapp/results.html',my_dict)
    #return render(request,'testapp/results.html',{'date':dt})

    #return HttpResponse('my name is lakhan')
----------------------------------------------------------------------
settings.py
-----------
"""
Django settings for ViewTemplate1 project.

Generated by 'django-admin startproject' using Django 2.1.3.

For more information on this file, see
https://docs.djangoproject.com/en/2.1/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/2.1/ref/settings/
"""

import os

# Build paths inside the project like this: os.path.join(BASE_DIR, ...)
BASE_DIR = os.path.dirname(os.path.dirname(os.path.abspath(__file__)))
# print(BASE_DIR)   #C:\Users\CHOTKA\Desktop\CorePython\DjangoApps\ViewTemplate1
TEMPLATE_DIR=os.path.join(BASE_DIR,'templates')
# print(TEMPLATE_DIR)  #C:\Users\CHOTKA\Desktop\CorePython\DjangoApps\ViewTemplate1\templates

# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/2.1/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = 'c5dtk13_yhf)7@a+88+y^qasavp%p#-u)9sy&-%lz+%j!lttp6'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

ALLOWED_HOSTS = []


# Application definition

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'testapp',

]

MIDDLEWARE = [
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

ROOT_URLCONF = 'ViewTemplate1.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [TEMPLATE_DIR],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

WSGI_APPLICATION = 'ViewTemplate1.wsgi.application'


# Database
# https://docs.djangoproject.com/en/2.1/ref/settings/#databases

DATABASES = {
    'default': {
        'ENGINE': 'django.db.backends.sqlite3',
        'NAME': os.path.join(BASE_DIR, 'db.sqlite3'),
    }
}


# Password validation
# https://docs.djangoproject.com/en/2.1/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]


# Internationalization
# https://docs.djangoproject.com/en/2.1/topics/i18n/

LANGUAGE_CODE = 'en-us'

TIME_ZONE = 'UTC'

USE_I18N = True

USE_L10N = True

USE_TZ = True


# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/2.1/howto/static-files/

STATIC_URL = '/static/'
------------------------------------------------------------------------------
results.py
------------
<!DOCTYPE html>
<html>
<body>

<h1>This is heading 1</h1>
<h2>This is heading 2{{mark}}</h2>
<h3>The Server Date and Time is : {{date}}</h3>
{{name}}
<h4>This is heading 4  {{rollno}}</h4>
<h5>This is heading 5</h5>
<h6>This is heading 6</h6>
<ol>
  <li>Name : {{name}}</li>
    <li>Roll No : {{rollno}}</li>
      <li>Marks : {{mark}}</li>
</ol>
</body>
</html>
-----------------------------------------------------
directory
-------------
C:\Users\CHOTKA\Desktop\PYTHON\DjangoApps\ViewTemplate1>tree /f
Folder PATH listing
Volume serial number is C223-DE11
C:.
│   db.sqlite3
│   manage.py
│
├───templates
│   └───testapp
│           results.html
│
├───testapp
│   │   admin.py
│   │   apps.py
│   │   models.py
│   │   tests.py
│   │   views.py
│   │   __init__.py
│   │
│   ├───migrations
│   │   │   __init__.py
│   │   │
│   │   └───__pycache__
│   │           __init__.cpython-37.pyc
│   │
│   └───__pycache__
│           admin.cpython-37.pyc
│           models.cpython-37.pyc
│           views.cpython-37.pyc
│           __init__.cpython-37.pyc
│
└───ViewTemplate1
    │   settings.py
    │   urls.py
    │   wsgi.py
    │   __init__.py
    │
    └───__pycache__
            settings.cpython-37.pyc
            urls.cpython-37.pyc
            wsgi.cpython-37.pyc
            __init__.cpython-37.pyc


C:\Users\CHOTKA\Desktop\PYTHON\DjangoApps\ViewTemplate1>






